<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>devPod | <%= current_page.data.title %></title>
    <%= feed_tag :atom, "#{canonical_url}#{blog.options.prefix.to_s}/feed.xml", title: "Atom Feed" %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%
    html_description = current_page.data.description
    html_description = current_page.data.title unless current_page.data.description

    html_perma_link = "#{canonical_url}#{blog.options.prefix.to_s}/#{request.path}"
    %>
    <%= partial "partials/meta", :locals => {
        :meta_keywords => "devpod, development, developer, podcast, türkçe, istanbul, türkiye, dev, ruby, python, php, perl, c, objective-c",
        :meta_description => html_description } %>
    <%= partial "partials/open_graph", :locals => {
        :html_title => current_page.data.title,
        :html_description => html_description,
        :perma_link => html_perma_link,
        } %>
    <%= partial "partials/icons" %>
    <%= partial "partials/html_shim" %>

    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.1.1/css/bootstrap.min.css">
    <link href="//netdna.bootstrapcdn.com/font-awesome/4.1.0/css/font-awesome.min.css" rel="stylesheet">
    <link href="//fonts.googleapis.com/css?family=Droid+Sans:400,700" rel="stylesheet">

    <%= stylesheet_link_tag 'application' %>
    <%= yield_content :css %>
  </head>
  <body>
    <%= yield %>

    <%= partial "partials/footer" %>

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.0/jquery.min.js"></script>
    <script src="//netdna.bootstrapcdn.com/bootstrap/3.1.1/js/bootstrap.min.js"></script>
    <%= javascript_include_tag 'retina-0.0.2' %>
    <%= javascript_include_tag 'app' %>
    <%= yield_content :javascript %>
    <%= partial "partials/google_analytics", :locals => {:analytics_id => "UA-6463685-36", :analytics_domain => "devpod.org"} %>
  </body>
</html>

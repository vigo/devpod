---
pageable: true
per_page: 10
---
<div class="block">
    <div class="container">
        <div class="row">
            <div class="col-sm-5">
                <%= partial "partials/logo_part" %>

                <h3>Arşiv</h3>
                <p id="archive-area">
                <% blog.articles.group_by {|a| a.date.year }.each do |year, articles| %>
                  <span class="label label-danger"><%= link_to "#{year} (#{articles.size} kayıt)", blog_year_path(year), :target => "_self" %></span>
                <% end %>
                </p>

            </div>
            <div class="col-sm-7">
                <h1>devPod<small>Türkçe Developer Podcast</small></h1>
                <p class="lead">
                  <strong>Mayıs 2012</strong>&rsquo;den beri ara ara yayınına devam eden kendi meşrebinde bir podcast!
                  Modern yazılım teknolojileri, popüler kütüphaneler, yazılım dilleri, konferanslar,
                  iOS ve MacOS gibi konular içerir.
                </p>

                <% if paginate && page_number > 1%>
                <h3>Sayfa: <%= page_number %></h3>
                <% end %>

                <ul class="list-unstyled list-general">
                    <%
                    page_articles.each_with_index do |article, i|
                      file_size = Filesize.from("#{article.metadata[:page]['length']} B").to_s("Mb")
                      concat '<li>'
                        concat link_to '<i class="fa fa-download"></i>', article.metadata[:page]['download'], :class => "btn btn-primary btn-xs pull-right", :title => "İndirmek için tıkla: #{file_size}"
                        concat link_to article.title, article, :target => "_self", :title => "Bölüm notları ve detaylar için tıklayın"
                      concat '</li>'
                    end
                    %>
                </ul>
                <%= custom_pager :paginate => paginate,
                                 :num_pages => num_pages,
                                 :current_page => page_number,
                                 :prev_page => prev_page,
                                 :next_page => next_page,
                                 :url_home => "/",
                                 :url_schema => "/sayfa/%d/"
                %>
            </div>
        </div>
    </div>
</div>
